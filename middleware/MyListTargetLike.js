// const User = require("../Database/Model/User")

// module.exports = async (req, userinfo) => {
//   // 요청으로 받은 유저의 이메일과 같은 유저를 찾는다.
//   // 그 유저의 list 에서 bucketid가 req.body.bucketid와 똑같은 것을 찾는다
//   // 찾은 해당 인덱스 의 like를 구조분해 한다 like = [...like, {'id':userinfo.nickname}]
//   // 그 후 list[i].like = like로 다시 변경하고 모두 변경된 값을
//   await User.findOne({ : })
// }